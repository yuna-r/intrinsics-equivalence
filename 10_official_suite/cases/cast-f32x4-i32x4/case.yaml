schema_version: 1
id: sse2.cast.f32x4.i32x4
description: reinterpret the full 128-bit payload from f32 lanes to i32 lanes

intel:
  symbol: intel_mm_castps_si128
  required_isa: [sse2]

openpower:
  symbol: power_mm_castps_si128
  required_isa: [power8, vsx]

signature:
  arguments:
    - {name: a, type: vector, element: f32, lanes: 4}
  return: {type: vector, element: i32, lanes: 4}

input_domain:
  exclude: []

comparison:
  mode: bit_exact

environment:
  fp_rounding_modes: [nearest_even]
  observe_fp_exceptions: false

tags: [endianness-sensitive]
