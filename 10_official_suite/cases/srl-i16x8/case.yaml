schema_version: 1
id: sse2.srl.i16x8.vector-count
description: logical right shift using the low 64-bit lane of a vector count

intel:
  symbol: intel_mm_srl_epi16
  required_isa: [sse2]

openpower:
  symbol: power_mm_srl_epi16
  required_isa: [power8, vsx]

signature:
  arguments:
    - {name: a, type: vector, element: i16, lanes: 8}
    - {name: count, type: vector, element: u64, lanes: 2}
  return: {type: vector, element: i16, lanes: 8}

input_domain:
  exclude: []

comparison:
  mode: bit_exact

environment:
  fp_rounding_modes: [nearest_even]
  observe_fp_exceptions: false

tags: [endianness-sensitive]
